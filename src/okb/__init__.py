PIPELINE_VERSION = "v3"  # v3: context-aware distillation (passport line in the
# prompt, question/resolution only for genuinely recorded decisions);
# v2: readable PDFs normalized via docling (heading structure)
